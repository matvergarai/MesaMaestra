/** Categoría de juegos de mesa (Estrategia, Familiar, Fiesta, Rol). */
export interface Categoria {  id: string;
  nombre: string;
  icono: string;
  descripcion: string;
  imagen: string;
  imagenRespaldo: string;
}
